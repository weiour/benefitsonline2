# Быстрый старт: Автоматизация сбора новостей

## 🚀 Самый простой способ (Windows)

### Вариант A: Windows Task Scheduler (Рекомендуется для Windows)

1. **Откройте PowerShell от имени администратора**

2. **Перейдите в директорию проекта:**
```powershell
cd C:\vitya9\lgoty-backend\backend
```

3. **Создайте задачу (запускается каждые 6 часов):**
```powershell
schtasks /Create /TN "LgotyNewsFetcher" /TR "python C:\vitya9\lgoty-backend\backend\manage.py fetch_news --limit 10" /SC HOURLY /MO 6 /F
```

4. **Проверьте задачу:**
```powershell
schtasks /Query /TN "LgotyNewsFetcher"
```

5. **Запустите задачу вручную для проверки:**
```powershell
schtasks /Run /TN "LgotyNewsFetcher"
```

**Готово!** Новости будут собираться автоматически каждые 6 часов.

---

## 🐍 Вариант B: Python скрипт с циклом (Простой)

1. **Запустите скрипт:**
```bash
cd C:\vitya9\lgoty-backend\backend
python run_news_fetcher.py
```

2. **Оставьте окно открытым** - скрипт будет работать в фоне и собирать новости каждые 6 часов.

3. **Для запуска в фоне** (Windows):
   - Создайте ярлык на `run_news_fetcher.py`
   - В свойствах ярлыка выберите "Свернуть в окно"
   - Добавьте ярлык в автозагрузку Windows

---

## ⚙️ Вариант C: Django-APScheduler (Для разработки)

1. **Установите зависимости:**
```bash
pip install django-apscheduler apscheduler
```

2. **Добавьте в `backend/settings.py`:**
```python
INSTALLED_APPS = [
    # ... существующие приложения
    'django_apscheduler',
    'benefits_news',
]

# Настройки для APScheduler
APSCHEDULER_DATETIME_FORMAT = "N j, Y, f:s a"
APSCHEDULER_RUN_NOW = True
```

3. **Примените миграции:**
```bash
python manage.py migrate
```

4. **Запустите планировщик:**
```bash
python manage.py run_scheduler
```

Планировщик будет работать в отдельном процессе и собирать новости:
- Каждый день в 9:00
- Каждые 6 часов

---

## 📋 Проверка работы

После настройки автоматизации проверьте:

```bash
python manage.py fetch_news --limit 5
```

Если команда работает, автоматизация тоже будет работать.

---

## 🔧 Управление задачами Windows

```powershell
# Просмотр всех задач
schtasks /Query

# Удаление задачи
schtasks /Delete /TN "LgotyNewsFetcher" /F

# Изменение расписания (каждые 3 часа вместо 6)
schtasks /Change /TN "LgotyNewsFetcher" /SC HOURLY /MO 3
```

---

## 💡 Рекомендации

- **Для Windows сервера**: Используйте Вариант A (Task Scheduler)
- **Для разработки**: Используйте Вариант C (APScheduler)
- **Для простоты**: Используйте Вариант B (Python скрипт)

Все варианты работают одинаково хорошо! Выберите тот, который вам удобнее.

